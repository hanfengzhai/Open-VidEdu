1. Generate sub-videos with speedup (default 1.5x) with subtitles (extracted from `plasticity_lecture_subtitles.txt`).
2. Save processed intermediate videos to `/code/interm_videos`.
3. Combine the videos in order `/code/interm_videos`.